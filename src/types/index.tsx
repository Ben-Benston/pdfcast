export interface Room {
    id: string
    room_code: string
    pdf_url: string
    pdf_name: string
    current_page: number
    zoom_level: string
    scroll_position: number
    created_at: string
}